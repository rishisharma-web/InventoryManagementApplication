package com.tcs.inventory.enums;

public enum POStatus {
    OPEN,           // After PO is submitted/confirmed
    CLOSED,         // All items received with invoice
    CANCELLED       // PO is cancelled
}
